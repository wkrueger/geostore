Schema:
  - Table name
  - Indexes
  - Filters
  - .map template
  - Version

Outputs:
  - gpkg importer (API and route)
  - feature query (API and route)
  - .map writer (API)

Must have:
  - swagger
  
Issues:
  - Dataset -> store delete cascade
  - Validate "code" format
  - Validation error responses bad
  - Save geography, not geometry

Fixme other:
  - Swagger - optional query params
  - Swagger - identified references

